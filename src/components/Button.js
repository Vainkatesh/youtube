const Button=({buttonName})=>{
    return (
        <div>
            <button className="px-5 py-2 m-2 bg-gray-200 rounded-lg cursor-pointer">{buttonName}</button>
        </div>
    );
}

export default Button;